<% include ../partials/header %>
<br>
<div class="jumbotron bg-link "> 
<h1 class="display-5 text-center">USERS LISTS</h1>  
<br>
<table class="table">
    <thead class="bg-info">
        <tr>
            <th>Users ID</th>
            <th>Email</th>
            <th>Password</th>
            <th> <a class="btn btn-success" href="/insert_user" role="button">Insert</a></th>
        </tr>
    </thead>
    <tbody id="myTable">
    <% users.forEach(function(user) { %>
        <tr>
            <td scope="row"><%= user.id %></td>
            <td><%= user.email %></td>
            <td><%= user.password %></td>
            <td>
            <a class="btn btn-warning" href="/users/<%= user.id%>" role="button">Edit</a>
            </td>
        </tr>
        <% }); %>
    </tbody>
   
</table>
 </div>
<% include ../partials/footer %>
